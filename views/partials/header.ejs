<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#000000" />
    <meta name="description" content="Web site created using create-react-app" />

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        html {
        scroll-behavior: smooth;
        }
    </style>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <!-- Brand stay on left and does not collapes -->
        <a class="navbar-brand mr-3" href="#">
            <img src="https://s3.eu-central-1.amazonaws.com/bootstrapbaymisc/blog/24_days_bootstrap/logo_white.png"
                width="30" height="30" alt="logo">
            The Social Network
        </a>
        <!-- collapse button -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- collapse item right-->
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="<%= homeLink %>">
                        <%= home %>
                    </a>
                </li>
                <li onclick="toggleForm('postForm')" id="showPostForm" class="nav-item" style="display:<%= addPost%>;">
                    <a class="nav-link" href="#">Add Post</a>
                </li>
            </ul>
        </div>

        <!-- collapse item center-->
        <div class="collapse navbar-collapse justify-content-center" id="collapsibleNavbar">
            <div class="navbar-nav" style="display: <%= showSearch%>;">
                <form class="form-inline nav-item">
                    <input class="form-control mr-sm-2" type="text" placeholder="Search for post">
                    <button class="btn btn-success" type="submit">Search</button>
                </form>
            </div>
        </div>

        <!-- collapse item  right with dropdown-->
        <div class="collapse navbar-collapse justify-content-end" id="collapsibleNavbar">
            <ul class="navbar-nav">
                <li class="nav-item dropdown mr-4">
                    <a class="dropdown-toggle nav-link" href="<%= userLink %>" role="button" data-toggle="dropdown">
                        <img src="<%= userImage %>" width="40" height="40" class="rounded-circle"> &nbsp <%= userName %>
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="<%= link1 %>">
                            <%= item1 %>
                        </a>
                        <a class="dropdown-item" href="<%= link2 %>">
                            <%= item2 %>
                        </a>
                        <a class="dropdown-item" href="<%= link3 %>">
                            <%= item3 %>
                        </a>
                        <hr>
                        <form style="display: <%= typeof deleteUser != 'undefined' ? 'block' : 'none' %>"
                              class="text-center  mb-2" 
                              action="/users/<%= typeof user != 'undefined' ? user.id : 'null'%>" 
                              method="POST">
                        <input type="hidden" name="_method" value="delete">
                        <button class="btn btn-danger ">
                            delete user?</button>
                       </form>     
                    </div>
                </li>
            </ul>
        </div>
    </nav>